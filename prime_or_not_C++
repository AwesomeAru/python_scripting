#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    // Numbers <= 1 are not prime
    if (n <= 1) {
        cout << "Number is not prime";
        return 0;
    }

    // Check divisibility up to sqrt(n)
    for (int i = 2; i * i <= n; i++) {
        if (n % i == 0) {
            cout << "Number is not prime";
            return 0;
        }
    }

    cout << "Number is prime";
    return 0;
}
